# Synthetic test input; never execute as a workflow.
name: unsupported-expression-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Title within an unmodeled expression
        shell: bash
        run: printf '%s\n' "${{ format('{0}', github.event.pull_request.title) }}"
