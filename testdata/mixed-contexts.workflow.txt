# Synthetic test input; never execute as a workflow.
name: mixed-contexts-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Title after context references
        shell: bash
        run: echo "${{ github.head_ref || github.ref_name }}" "${{ secrets.example_token }}" "${{ github.event.pull_request.title }}"
      - name: Body before context references
        shell: sh
        run: echo "${{ github.event.pull_request.body }}" "${{ github.workspace }}" "${{ env.example_value }}"
