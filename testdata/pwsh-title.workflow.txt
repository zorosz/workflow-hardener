# Synthetic test input; never execute as a workflow.
name: unsupported-shell-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Title in PowerShell
        shell: pwsh
        run: Write-Output "${{ github.event.pull_request.title }}"
