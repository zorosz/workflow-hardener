# Synthetic test input; never execute as a workflow.
name: runner-default-title-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Title with the runner default shell
        run: printf '%s\n' "${{ github.event.pull_request.title }}"
