# Synthetic test input; never execute as a workflow.
name: context-literals-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Contexts and expression string literals
        shell: bash
        run: |
          echo "${{ github.head_ref || github.ref_name }}" "${{ secrets.example_token }}"
          echo "${{ 'It''s }} || ${{ github.event.pull_request.body }}' }}"
