# Synthetic test input; never execute as a workflow.
name: risk-pattern-fixture
'on': pull_request
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Print a title
        shell: bash
        run: printf '%s\n' "${{ github.event.pull_request.title }}"
